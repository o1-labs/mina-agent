# Project state written by mina-agent (derived graph, compiled describe-dune,
# run logs, discussion notes). Reproducible or personal; never committed.
state/
# Python
__pycache__/
*.egg-info/
.venv/
# secrets sourced into harness sessions (see .envrc.example)
.envrc
